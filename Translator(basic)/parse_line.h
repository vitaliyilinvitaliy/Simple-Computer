#pragma once

#include <stdlib.h>
#include <stdio.h>
#include <string.h>


int if_pars(char *com, char *par, char *a);
int calcpars(char *a);
int let_merge(const char *from_let_line, char *to_let_line);